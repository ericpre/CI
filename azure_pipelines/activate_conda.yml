steps:
- bash: |
    echo "##vso[task.prependpath]$(MAMBAFORGE_PATH)/bin"
  displayName: Add conda to PATH (Linux)
  condition: eq(variables['Agent.OS'], 'Linux')

- bash: |
    echo "##vso[task.prependpath]$(MAMBAFORGE_PATH)/bin"
    sudo chown -R $USER $(MAMBAFORGE_PATH)
  displayName: Add conda to PATH (OS X)
  condition: eq(variables['Agent.OS'], 'Darwin')

- powershell: |
    Write-Host "##vso[task.prependpath]$env:MAMBAFORGE_PATH\Scripts"
  displayName: Add conda to PATH (Windows)
  condition: eq(variables['Agent.OS'], 'Windows_NT')

